# lambda-firehose-converter-pinpoint-stream
Lambda Function for Firehose Converter of Pinpoint Stream

Allow only `event_type` == `_campaign.opened_notification` and compress its payload.

Drop other records.
